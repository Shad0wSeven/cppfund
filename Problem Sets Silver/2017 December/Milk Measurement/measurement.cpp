#include <iostream>
#include <fstream>
#include <map>
#include <set>
#include <vector>
#include <algorithm>
#include <string>
#define ll long long

using namespace std;

int main () {
	ifstream fin("measurement.in");
	ofstream fout("measurement.out");
	int N, G;
	fin >> N >> G;
	map<int, pair<int, int> > maze;
	for(int i = 0; i < N; i++) {
		int a, b, c;
		fin >> a >> b >> c;
		map[a] = make_pair(b, c);
	}
	
	mkdir
	for(int i = 0; i < N; i++)

	return 0;
}

